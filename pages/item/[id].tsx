import React, { useEffect } from 'react'

const itemDetail = () => {

    useEffect(() => {
        // appscheme 여부 따지고 os 여부 따지고  playstore or appstore

        window.location.replace('shoppingcat://')
    }, [])

    return (
        <div>

        </div>
    )
}

export default itemDetail
